package com.ashurudra.wallpapercycler.ui.theme

import androidx.compose.material3.Typography

// Default Material 3 type ramp (system font). Revisit if the alarm-list look needs
// a distinct display face once real screens are built out in Phase 6.
val WallpaperCyclerTypography = Typography()
